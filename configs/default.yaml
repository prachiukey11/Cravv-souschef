data:
  root: dataset
  segmentation: {num_classes:2}
  detection: {num_classes:2}
  classification: 
    class_names: ['0','0.25','0.5','0.75','1.0']
    num_classes: 5

train:
  image_size: 512
  batch_size_seg: 4
  batch_size_det: 4
  batch_size_cl: 16
  epochs: 30
  head_lr: 1.0e-3
  backbone_lr: 1.0e-5

loss_weights:{seg:1.0, det:1.0, cls:0.5 }